
import { gradeAvg } from 'rector/helpers/grade-avg';
import { module, test } from 'qunit';

module('Unit | Helper | grade avg');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gradeAvg([42]);
  assert.ok(result);
});

