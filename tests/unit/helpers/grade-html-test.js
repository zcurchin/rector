
import { gradeHtml } from 'rector/helpers/grade-html';
import { module, test } from 'qunit';

module('Unit | Helper | grade html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gradeHtml([42]);
  assert.ok(result);
});

