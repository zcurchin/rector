
import { gradeBox } from 'rector/helpers/grade-box';
import { module, test } from 'qunit';

module('Unit | Helper | grade box');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gradeBox([42]);
  assert.ok(result);
});

