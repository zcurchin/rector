
import { msDate } from 'rector/helpers/ms-date';
import { module, test } from 'qunit';

module('Unit | Helper | ms date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = msDate([42]);
  assert.ok(result);
});

