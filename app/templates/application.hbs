{{#if session.isAuthenticated}}
  {{#app-main
    signOut="signOut"
    signUpRestaurant="signUpRestaurant"
    goToSignIn="goToSignIn"
  }}
    {{outlet}}
  {{/app-main}}

{{else}}
  <div class="gate-page">
    {{outlet}}
  </div>
{{/if}}
