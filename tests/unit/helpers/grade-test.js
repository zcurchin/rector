
import { grade } from 'rector/helpers/grade';
import { module, test } from 'qunit';

module('Unit | Helper | grade');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = grade([42]);
  assert.ok(result);
});

